---
- name: Validate cluster health and prerequisites
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Run validate-cluster checks
      ansible.builtin.include_role:
        name: validate_cluster
