version: "2.0"
backingStore: vault

bootstrap_secrets:
  - name: only-bootstrap
    targetNamespaces:
      - validated-patterns-secrets
    vaultPrefixes:
      - hub
    fields:
      - name: k
        value: v
        onMissingValue: error
