version: "2.0"
backingStore: vault

bootstrap_secrets:
  - name: config-demo
    targetNamespaces:
      - default
    vaultPrefixes:
      - hub
    fields:
      - name: a
        value: x
        onMissingValue: error

secrets:
  - name: config-demo
    vaultPrefixes:
      - hub
    fields:
      - name: b
        value: y
        onMissingValue: error
