version: "2.0"

backingStore: vault

secrets:
  - name: bad-bootstrap-value
    bootstrap: nonsense
    targetNamespaces:
      - openshift-gitops
    fields:
      - name: token
        value: x
        onMissingValue: error
